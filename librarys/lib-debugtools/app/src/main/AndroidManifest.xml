<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medlinker.dt.test">

    <application
        android:name=".DTApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Libdebugtools">
        <activity android:name=".DTTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".act.DTLaneTestActivity" android:screenOrientation="portrait"/>
        <activity android:name=".act.DTAopTestActivity" android:screenOrientation="portrait"/>
        <activity android:name=".act.DTCrashTestActivity" android:screenOrientation="portrait"/>
    </application>

</manifest>