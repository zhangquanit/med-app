<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/input_edit_height"
    android:background="@drawable/input_edit_bg"
    android:paddingLeft="@dimen/input_edit_space_h"
    android:paddingRight="@dimen/input_edit_space_h">

    <com.medlinker.widget.search.SearchEditText
        android:id="@+id/account_edit_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="4dp"
        android:layout_weight="1"
        android:background="@null"
        android:digits="0123456789"
        android:gravity="bottom"
        android:imeOptions="actionNext"
        android:inputType="phone"
        android:maxLength="11"
        android:maxLines="1"
        android:paddingTop="@dimen/input_edit_space_v"
        android:paddingBottom="@dimen/input_edit_space_v"
        android:textColor="@color/search_text_color"
        android:textColorHint="@color/search_text_color_hint"
        android:textCursorDrawable="@drawable/search_input_cursor"
        android:textSize="@dimen/input_text_size"
        tools:hint="输入您的内容" />

    <ImageView
        android:id="@+id/iv_delete"
        android:layout_width="@dimen/search_icon_size"
        android:layout_height="@dimen/search_icon_size"
        android:layout_gravity="center_vertical"
        android:src="@mipmap/search_delete_gray"
        android:visibility="gone"
        tools:visibility="visible" />

</LinearLayout>